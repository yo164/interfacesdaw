export class Planet {

    constructor(
        id,
        name,
        description,
        image,
        deletedAt
    ) {
        this.id = id;
        this.name = name;
        this.description = description;
        this.image = image;
        this.deletedAt = deletedAt;
    }
}
